<form method="post" action="{{ path('talk_delete', {'id': talk.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ talk.id) }}">
    <button class="btn">Delete</button>
</form>
